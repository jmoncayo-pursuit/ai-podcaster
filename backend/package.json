{
  "dependencies": {
    "@google/genai": "^0.9.0",
    "@speechify/api-sdk": "^2.5.1",
    "compression": "^1.8.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "fluent-ffmpeg": "^2.1.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.1",
    "@types/fluent-ffmpeg": "^2.1.27",
    "@types/node": "^22.14.1",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "dev": "nodemon --watch src --exec 'node --no-deprecation -r ts-node/register src/server.ts'",
    "start": "node --no-deprecation -r ts-node/register src/server.ts",
    "postinstall": "apt-get update && apt-get install -y ffmpeg || true"
  }
}
